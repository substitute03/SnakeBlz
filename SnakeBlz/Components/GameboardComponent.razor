@using SnakeBlz.Components

<div class="container d-flex justify-content-center">

    <div class="gameboard">

        @for (int y = 1; y <= GameboardSize; y++)
        {
            for (int x = 1; x <= GameboardSize; x++)
            {
                <CellComponent @ref="SetComponentRef" X="x" Y="y"></CellComponent>                  
            }
        }

    </div>
</div>